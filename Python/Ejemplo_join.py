#Ejemplo para ver el funcionamiento de string.join(iterable)
#Une una lista de strings pasada por parámetro con el string sobre el que se aplica
#Se puede aplicar con el string vacío: "".join(iterable)
sdh = ["Jaime", "Pulido", "Andrés"]
print("-".join(sdh))